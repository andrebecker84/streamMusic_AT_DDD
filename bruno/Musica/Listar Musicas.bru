meta {
  name: Listar Musicas
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/musicas
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200 && res.body.length > 0) {
    bru.setEnvVar("musicaId", res.body[0].id);
  }
}
